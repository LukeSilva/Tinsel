import FileIO
import ConsoleIO

int test(String mystring,bool mybool, int myint)
{
	Console my_console;
	if (!my_console.open()) return 1;
	if (!my_console.putstring(mystring)) return 2;
	if (!my_console.putchar('\n')) return 2;

	if (mybool)
	{
		if(!my_console.putint(myint)) return 2;
		if(!my_console.putchar('\n')) return 2;
	}
	my_console.close();
	return myint;
}

int add(int a, int b)
{
	return a + b;
}
